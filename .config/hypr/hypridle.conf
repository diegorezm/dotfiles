general {
    lock_before_sleep = true  # Lock screen before suspend
}

listener {
    timeout = 300               # 5 minutes (300 seconds)
    on-timeout = hyprlock       # Lock screen first
}

listener {
    timeout = 600               # 10 minutes (600 seconds)
    on-timeout = systemctl suspend  # Suspend computer
}
